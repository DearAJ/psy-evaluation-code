<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.ww.mapper.ManagerMapper">

    <update id="updateManager">
        UPDATE manager SET name = #{name}, phone = #{phone}, email = #{email}, sex = #{sex} WHERE username = #{username} AND deleted = 0
    </update>

    <update id="updateSchoolName">
        UPDATE manager SET school = #{school_name} WHERE username = #{school_login}
    </update>

    <update id="updateSchoolManagerInfo">
        UPDATE manager SET name = #{name}, phone = #{phone} WHERE username = #{school_login}
    </update>

</mapper>